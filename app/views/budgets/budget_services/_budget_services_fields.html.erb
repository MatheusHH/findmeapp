<div class='nested-fields'>
  <div class="field">
    <%= f.label :service %>
    <%= f.select(:service_id, Service.where(user_id: current_user.id ).collect {|p| [ p.name, p.id ] }, {prompt: "Selecione o serviço"}, { class: "form-control" })%>
  </div>
  <%= link_to_remove_association "Remove service", f, class: "btn btn-outline btn-danger btn-xs pull-right" %>
</div>